type User {
  id: ID!
  username: String!
  email: String!
}

input SignUpInput {
  username: String!
  email: String!
  password: String!
}

type Query {
  getUser(id: ID!): User
  getUsers: [User]
}

type Mutation {
  signUp(input: SignUpInput!): User
}
